<Page x:Class="PCAN_AutoCar_Test_Client.View.TestRealtimePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PCAN_AutoCar_Test_Client.View"
      xmlns:usercontrols="clr-namespace:PCAN_AutoCar_Test_Client.UserControls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TestRealtimePage" d:Background="AliceBlue">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <usercontrols:PCanClientUsercontrol x:Name="pcanClientUsercontrol" Grid.Row="0"/>
        <Grid Grid.Row="1">
            
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <GroupBox>
                <DockPanel Background="AliceBlue">
                    <DockPanel.Resources>
                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsUploading}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsUploading}" Value="False">
                                    <Setter Property="IsEnabled" Value="True"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DockPanel.Resources>
                    <Label Content="文件路径:" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="5"/>
                    <TextBox x:Name="FilePathTextBox" Width="400" Margin="5" VerticalAlignment="Center" HorizontalAlignment="Left" IsReadOnly="True"/>
                    <Button x:Name="BrowseFileButton" Content="浏览" Width="75" Margin="5" VerticalAlignment="Center" HorizontalAlignment="Left" />
                    <Button x:Name="BeTestButton" Content="开始测试" Width="90" Margin="5" VerticalAlignment="Center" HorizontalAlignment="Left" />
                </DockPanel>
            </GroupBox>
            <DataGrid x:Name="TestExcelDataGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False">
                <DataGrid.Resources>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Pass}" Value="True">
                                <Setter Property="Background" Value="Green"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="序号" Binding="{Binding Index}"/>
                    <DataGridTextColumn Header="发送ID" Binding="{Binding SendId}"/>
                    <DataGridTextColumn Header="发送数据" Binding="{Binding SendData}"/>
                    <DataGridTextColumn Header="回复ID" Binding="{Binding RecvId}"/>
                    <DataGridTextColumn Header="回复数据" Binding="{Binding RecvData}"/>
                    <DataGridTextColumn Header="最大值" Binding="{Binding MinData}"/>
                    <DataGridTextColumn Header="最小值" Binding="{Binding MaxData}"/>
                    <DataGridTextColumn Header="是否通过" Binding="{Binding Pass}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Page>
