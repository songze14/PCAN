<Window
    x:Class="PCAN.View.Windows.SignalSettingWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PCAN.View.Windows"
    xmlns:materialDesignMahApps="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="线调整"
    Width="800"
    Height="450"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid>
        <Grid.Resources>
            <Style TargetType="Label">
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="FontSize" Value="15" />
                <Setter Property="FontWeight" Value="Black" />
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Label Content="线" />
        <ComboBox
            x:Name="SignalComboBox"
            Grid.Column="1"
            Margin="5"
            DisplayMemberPath="LegendText"
            FontSize="15" />
        <materialDesignMahApps:ColorPicker
            x:Name="signalColorPicker"
            Grid.Row="1"
            Grid.ColumnSpan="2" />
        <Label
            Grid.Row="2"
            Content="增益"
            ToolTip="只增益Y轴" />
        <TextBox
            x:Name="GainTextBox"
            Grid.Row="2"
            Grid.Column="1"
            Margin="5" />
        <Label Grid.Row="3" Content="X偏移" />
        <TextBox
            x:Name="XOFFTextBox"
            Grid.Row="3"
            Grid.Column="1"
            Margin="5" />
        <Label Grid.Row="4" Content="Y偏移" />
        <TextBox
            x:Name="YOFFTextBox"
            Grid.Row="4"
            Grid.Column="1"
            Margin="5" />
        <Grid Grid.Row="5" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />

            </Grid.ColumnDefinitions>
            <Grid.Resources>
                <Style BasedOn="{StaticResource MaterialDesignFlatMidBgButton}" TargetType="Button">
                    <Setter Property="Width" Value="60" />
                    <Setter Property="Height" Value="40" />
                </Style>
            </Grid.Resources>
            <Button x:Name="RestButton" Content="重置" />
            <Button
                x:Name="BackButton"
                Grid.Column="2"
                Click="BackButton_Click"
                Content="取消" />

        </Grid>
    </Grid>
</Window>
