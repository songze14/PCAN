<Window
    x:Class="PCAN.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PCAN"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:usercontrols="clr-namespace:PCAN.UserControls"
    xmlns:vm="clr-namespace:PCAN.ViewModel"
    d:Height="450"
    d:Width="1200"
    Closed="Window_Closed"
    WindowState="Maximized"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70" />
            <ColumnDefinition />
            <ColumnDefinition Width="400" />
        </Grid.ColumnDefinitions>
        <!--#region菜单-->
        <!--<Border
            x:Name="left"
            Grid.Column="0"
            Width="200"
            BorderBrush="CadetBlue"
            BorderThickness="2"
            Visibility="Visible">
            <Menu>
                <Menu.Resources>
                    <Style TargetType="MenuItem">
                        <Setter Property="Background" Value="#FF00FFFF" />
                        <Setter Property="FontFamily" Value="微软雅黑" />
                        <Setter Property="FontSize" Value="15pt" />
                        <Setter Property="FontWeight" Value="Bold" />
                        <Setter Property="Height" Value="50" />
                    </Style>
                </Menu.Resources>
                <Menu.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </Menu.ItemsPanel>
                <MenuItem
                    x:Name="SysTemSettings"
                    Click="SysTemSettings_Click"
                    Header="系统设置" />

                <MenuItem
                    x:Name="BasicFunctions"
                    Click="MenuItem_Click"
                    Header="基本功能" />
                <MenuItem
                    x:Name="PCANDataParse"
                    Click="PCANDataParse_Click"
                    Header="PCAN信息解析" />
                <MenuItem
                    x:Name="Upload"
                    Click="Upload_Click"
                    Header="升级" />
                <MenuItem
                    x:Name="DeviceParmTuning"
                    Click="DeviceParmTuning_Click"
                    Header="设备参数调试" />
                <MenuItem
                    x:Name="DataMonitoring"
                    Click="DataMonitoring_Click"
                    Header="数据监控" />

            </Menu>
        </Border>-->
        <ToolBarTray Orientation="Vertical">
            <ToolBar ClipToBounds="False" Style="{StaticResource MaterialDesignToolBar}">
                <Button
                    Click="SysTemSettings_Click"
                    Content="{materialDesign:PackIcon Kind=CogOutline}"
                    ToolTip="系统设置" />
                <Button
                    Click="MenuItem_Click"
                    Content="{materialDesign:PackIcon Kind=StoreSearchOutline}"
                    ToolTip="基本功能" />
                <Button
                    Click="Upload_Click"
                    Content="{materialDesign:PackIcon Kind=UploadMultipleOutline}"
                    ToolTip="升级" />
                <Button
                    Click="DeviceParmTuning_Click"
                    Content="{materialDesign:PackIcon Kind=WrenchOutline}"
                    ToolTip="设备参数调试" />
                <Button
                    Click="DataMonitoring_Click"
                    Content="{materialDesign:PackIcon Kind=ChartBellCurve}"
                    ToolTip="数据监控" />

            </ToolBar>

        </ToolBarTray>
        <Frame
            Name="navWin"
            Grid.Column="1"
            Margin="0,0,0,0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch"
            Background="#FF1E3853"
            NavigationUIVisibility="Hidden"
            ScrollViewer.HorizontalScrollBarVisibility="Visible"
            ScrollViewer.VerticalScrollBarVisibility="Visible" />
        <!--#endregion-->
        <!--#region连接设置-->
        <usercontrols:UILogsView x:Name="uilogView" Grid.Column="2" />
    </Grid>
</Window>
